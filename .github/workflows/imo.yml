name: IMO Tests

on:
  push:
    branches: [main]
    paths:
      - "tests/vv/test_imo_*.py"
      - "tests/vv/vv_helpers.py"
      - "requirements-vv.txt"
      - ".github/workflows/imo.yml"
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch: {}

jobs:
  imo-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - uses: astral-sh/setup-uv@v5

    - name: Install dependencies
      run: uv pip install --system -r requirements-vv.txt

    - name: Run IMO tests
      run: python -m pytest tests/vv/test_imo_*.py -v --junitxml=results/imo-tests.xml

    - name: Upload results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: imo-test-results
        path: results/imo-tests.xml

    - name: Test summary
      if: always()
      uses: test-summary/action@v2
      with:
        paths: results/imo-tests.xml
